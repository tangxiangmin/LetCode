/**
 * @param {number} K
 * @param {number} N
 * @return {number}
 * 思路：由于存在K的限制，因此使用二分查找有局限，本题使用动态规划
 */
var superEggDrop = function (K, N) {

};

var K = 2, N = 6 // 3
var res = superEggDrop(K, N)
console.log(res)

// dp(K, N)  dp(K -1, N/2)

