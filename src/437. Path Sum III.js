/**
 * Created by admin on 2017/4/20.
 */
// 在二叉树中找到某条节点之和等于给定参数的路径上
// 这个就是基本的深度优先算法了
var pathSum = function(root, sum) {
    var res = [];

    // getSum(root);
    //
    // function getSum(root) {
    //     var sum = 0;
    //     if (root === null){
    //         return 0;
    //     }
    //     sum += root.val + getSum(root.left);
    //     res.push(sum);
    //     return sum;
    // }
    return res;
};