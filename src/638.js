/**
 * @param {number[]} price
 * @param {number[][]} special
 * @param {number[]} needs
 * @return {number}
 */
var shoppingOffers = function(price, special, needs) {

};

var price = [2,5], special = [[3,0,5],[1,2,10]], needs = [3,2]

var ans = shoppingOffers(price, special, needs)

console.log(ans)