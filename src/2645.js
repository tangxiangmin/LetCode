/**
 * @param {string} word
 * @return {number}
 */
var addMinimum = function (word) {
    let ans = 0
    let cur = 0
    const chars = ['a', 'b', 'c']


};
var word = "aaa"
var ans = addMinimum(word)
console.log(ans)