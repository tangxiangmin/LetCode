/**
 * @param {number[]} prices
 * @param {number} fee
 * @return {number}
 */
// 动态规划
var maxProfit = function (prices, fee) {

};

// dp[0]= 0
// dp[1]= Math.max(p1 - p0 - fee, 0)
// dp[2]

// 对于某一天有三种状态，买入、卖出、不进行操作，使用dp[i]表示第i天的最大收益，则
//